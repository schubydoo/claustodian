{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    "helpers:pinGitHubActionDigests",
    ":dependencyDashboard",
    ":semanticCommits"
  ],
  "timezone": "Etc/UTC",
  "schedule": ["before 7am on monday"],
  "labels": ["dependencies"],
  "description": "Manual-merge for now: every update opens a PR for review (no automerge), subject to the same gates as any PR (Greptile 5/5 + resolved threads, validate + security checks, Codecov patch, up-to-date branch). Flip on platformAutomerge + per-rule automerge later to auto-land green digest/lockfile updates.",
  "vulnerabilityAlerts": {
    "enabled": true,
    "labels": ["security"]
  },
  "lockFileMaintenance": {
    "enabled": true,
    "schedule": ["before 7am on monday"]
  },
  "ignorePaths": ["**/node_modules/**"],
  "customManagers": [
    {
      "customType": "regex",
      "description": "Track the OSV-Scanner binary version pinned in the scheduled scan. Renovate bumps OSV_VERSION; update OSV_SHA256 to match (the workflow's sha256sum -c fails loudly until you do — the hosted Renovate App can't recompute a binary checksum).",
      "managerFilePatterns": ["^\\.github/workflows/osv-scanner\\.yml$"],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>\\S+) depName=(?<depName>\\S+)\\s+OSV_VERSION:\\s*['\"](?<currentValue>v[0-9][^'\"]*)['\"]"
      ],
      "versioningTemplate": "semver"
    }
  ],
  "packageRules": [
    {
      "description": "Roll non-major GitHub Actions version updates into one weekly PR.",
      "matchManagers": ["github-actions"],
      "matchUpdateTypes": ["minor", "patch"],
      "groupName": "github-actions (non-major)"
    },
    {
      "description": "Group GitHub Actions digest re-pins (same version, new SHA) — keeps the SHA pins current via helpers:pinGitHubActionDigests.",
      "matchManagers": ["github-actions"],
      "matchUpdateTypes": ["digest"],
      "groupName": "github-actions digests"
    },
    {
      "description": "Group the security-tooling actions (security.yml scanners + scorecard + osv) so they review together.",
      "matchManagers": ["github-actions"],
      "matchPackageNames": [
        "github/codeql-action",
        "actions/dependency-review-action",
        "gitleaks/gitleaks-action",
        "ossf/scorecard-action",
        "google/osv-scanner-action"
      ],
      "groupName": "security tooling actions"
    }
  ]
}
